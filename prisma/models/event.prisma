model Event {
  id          String   @id @default(uuid())
  title       String   @unique
  date        DateTime
  location    String
  category    String
  description String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("events")
}
